import { configureStore } from "@reduxjs/toolkit";

export const store = configureStore({
	reducer: {
		// first we will import the slice then give refrence of the reducer in this reducer of the store
	},
});
